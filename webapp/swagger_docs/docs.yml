Bubble sort algorithm with time complexity of O(n)
--- 
tags:
  - Bubble sort
consumes:
    - application/json
parameters:  
    - in: body
      name: bubble sort
      schema:
        typa: object 
        example: { "unsorted-list" : [6, 2, [4, 3], [[[5], Null], 1]]}
responses:
    201:
        description: Returns a sorted list. 
    401:
        description: Must not be empty.
    401: 
        description: Elements in list must be less then Max Length.
   

   
